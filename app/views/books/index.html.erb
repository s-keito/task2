<h1>Books</h1>

 <h4>title</h4>
 <h4>body</h4>
<% @books.each do |book| %>

 
  <span><%= book.title %></span>
  
  
  <span><%= book.body %></span>
  
  
  <span>
    <%= link_to "Show", book_path(book.id) %>
  </span>
  
  <span>
    <%= link_to "Edit", edit_book_path(book.id) %>
  </span>
  
 
  
<% end %>
